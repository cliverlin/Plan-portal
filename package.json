{
  "name": "ok-plan-portal",
  "version": "1.0.0",
  "private": true,
  "description": "OfficeKeeper planning artifact portal",
  "scripts": {
    "build": "node scripts/build-portal.js",
    "test": "node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=20"
  }
}
